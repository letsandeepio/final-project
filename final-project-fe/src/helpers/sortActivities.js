import minuteTimeConvert from "./minuteTimeConvert";

export default function sortActivities(activitiesArray, category, time) {
  if (category === "what should i do?") {

  }
  
  minuteTimeConvert(time);
  return activitiesArray;
}